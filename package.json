{
	"name": "@jsp/monorepo",
	"engines": {
		"node": ">=24"
	},
	"packageManager": "pnpm@10.22.0",
	"scripts": {
		"dev": "turbo dev --filter=jsp...",
		"build": "turbo build --filter=jsp...",
		"extension-vscode:dev": "turbo vscode:dev --filter=extension-vscode...",
		"extension-vscode:build": "turbo vscode:build --filter=extension-vscode...",
		"test": "turbo build --filter=vanilla..."
	},
	"devDependencies": {
		"turbo": "^2.6.3"
	}
}
