{
	"name": "@jsp/monorepo",
	"engines": {
		"node": ">=24"
	},
	"packageManager": "pnpm@10.26.1",
	"scripts": {
		"dev": "turbo dev",
		"build": "turbo build",
		"jsp:dev": "turbo dev --filter=jsp... --filter=!tester",
		"jsp:build": "turbo build --filter=jsp... --filter=!tester",
		"jsp:test": "turbo test --filter=jsp...",
		"extension-vscode:dev": "turbo vscode:dev --filter=extension-vscode...",
		"extension-vscode:build": "turbo vscode:build --filter=extension-vscode..."
	},
	"devDependencies": {
		"turbo": "^2.7.1"
	}
}
