---
title: Overview
lastUpdated: 2026-02-08
---

import { pages } from './pages';

import Table from './table.astro';

## Syntax

<Table
	pages={pages.filter((page) => page.frontmatter.proposalType === 'Syntax')}
	hideCols={['proposalType']}
/>

## Polyfills

<Table
	pages={pages.filter((page) => page.frontmatter.proposalType === 'Polyfill')}
	hideCols={['proposalType']}
/>

## Runtime

<Table
	pages={pages.filter((page) => page.frontmatter.proposalType === 'Runtime')}
	hideCols={['proposalType']}
/>
