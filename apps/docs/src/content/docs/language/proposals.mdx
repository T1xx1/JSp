---
title: Proposals
lastUpdated: 2026-02-08
---

import { allPages } from './pages';

import Percent from './percent.astro';
import PercentTip from './percenttip.astro';
import Table from './table.astro';

:::note[JS+ currently supports <Percent pages={allPages} /> of all proposals]
:::

## TC39 proposals

### Stage 3 • <PercentTip pages={allPages.filter((proposal) => proposal.frontmatter.support.tc39?.stage === 3)} />

<Table
	pages={allPages.filter((proposal) => {
		return proposal.frontmatter.support.tc39?.stage === 3;
	})}
	hideCols={['tc39']}
/>

### Stage 2.7 • <PercentTip pages={allPages.filter((proposal) => proposal.frontmatter.support.tc39?.stage === 2.7)} />

<Table
	pages={allPages.filter((proposal) => {
		return proposal.frontmatter.support.tc39?.stage === 2.7;
	})}
	hideCols={['tc39']}
/>

### Stage 2 • <PercentTip pages={allPages.filter((proposal) => proposal.frontmatter.support.tc39?.stage === 2)} />

<Table
	pages={allPages.filter((proposal) => {
		return proposal.frontmatter.support.tc39?.stage === 2;
	})}
	hideCols={['tc39']}
/>

### Stage 1 • <PercentTip pages={allPages.filter((proposal) => proposal.frontmatter.support.tc39?.stage === 1)} />

<Table
	pages={allPages.filter((proposal) => {
		return proposal.frontmatter.support.tc39?.stage === 1;
	})}
	hideCols={['tc39']}
/>

### Stage 0 • <PercentTip pages={allPages.filter((proposal) => proposal.frontmatter.support.tc39?.stage === 0)} />

<Table
	pages={allPages.filter((proposal) => {
		return proposal.frontmatter.support.tc39?.stage === 0;
	})}
	hideCols={['tc39']}
/>

### Inactive • <PercentTip pages={allPages.filter((proposal) => proposal.frontmatter.support.tc39?.stage === 'Inactive')} />

<Table
	pages={allPages.filter((proposal) => {
		return proposal.frontmatter.support.tc39?.stage === 'Inactive';
	})}
	hideCols={['tc39']}
/>

## Others • <PercentTip pages={allPages.filter((proposal) => proposal.frontmatter.support.tc39?.value === undefined)} />

<Table
	pages={allPages.filter((proposal) => {
		return proposal.frontmatter.support.tc39?.value === undefined;
	})}
	hideCols={['tc39']}
/>

---

#### Resources

- [TC39 proposals](https://github.com/tc39)
- [TC39 process document](https://tc39.es/process-document)

#### Related

- [Proposals by type](/language/proposalsbytype)
