---
title: "'Cause ESNext is not enough"
template: splash
hero:
  tagline: An opinionated TypeScript superset for the next generation of JavaScript
  image:
    file: ../../assets/logo.png
  actions:
    - text: Get started
      link: /overview
      icon: right-arrow
lastUpdated: 2026-02-08
---

import Diff from './language/diff.astro';

#### [Pipelines](/language/syntax/pipelineoperator)

<Diff>
	```ts
	Object.keys(envars)
		.map(envar => `${envar}=${envars[envar]}`)
		.join(' ')
		|> `$ ${%}`
		|> chalk.dim(%, 'node', args.join(' '))
		|> console.log(%)
    ```

    ```ts
    console.log(
    	chalk.dim(
    		`$ ${Object.keys(envars)
    			.map(envar =>
    				`${envar}=${envars[envar]}`)
    			.join(' ')
    		}`,
    		'node',
    		args.join(' ')
    	)
    );
    ```

</Diff>

#### [Decorators](/language/syntax/decorators)

<Diff>
	```ts
	class C {
		message = "hello!";

    	@bound
    	m() {
    		console.log(this.message);
    	}
    }

    function bound(value, { name, addInitializer }) {
    	addInitializer(function () {
    		this[name] = this[name].bind(this);
    	});
    }
    ```

    ```ts
    // TypeScript supports decorators behind an experimental flag
    // JavaScript doesn't support decorators yet
    ```

</Diff>

<br />

<div class="flex justify-center">
	##### [And much more cool stuff...](/language)
</div>
